# AGENTS.md — Agent Guide for this Repo

This file orients AI agents so you don’t re‑scan the repo each run. Treat it as the canonical map and workflow. Scope: the entire repository unless noted.

## Quick Start (Agent)
- Read only these first: `AGENTS.md`, `docusaurus.config.js`, `sidebars.js`, `package.json`.
- For content work, look in: `docs/` (EN) and `i18n/az/docusaurus-plugin-content-docs/current/` (AZ).
- Ignore heavy/derived dirs: `node_modules/`, `build/`, `.docusaurus/`.
- Validate with scripts in `package.json` (see Commands) — do not add tools without request.

## Repo Overview
- Type: Docusaurus v3 documentation site for InfoSec tutorials.
- Node: `>=20` (see `package.json` → `engines`).
- i18n: English (`en`, default) and Azerbaijani (`az`). `routeBasePath: '/'`.
- Hosting: GitHub Pages (`url` + `baseUrl` set in `docusaurus.config.js`).

### Key Paths
- `docs/` — Main English content. Top pages: `intro.md`, `about.md`.
- `i18n/az/docusaurus-plugin-content-docs/current/` — AZ translations mirroring `docs/` structure.
- `sidebars.js` — Sidebar categories, mostly `autogenerated` from folder names.
- `docusaurus.config.js` — Site config, navbar, locales, theming.
- `static/` — Static assets, e.g., `img/shield.png`.
- `src/css/custom.css` — Custom styles.
- `src/components/DocState/index.js` — Doc maturity/review banner rendered on lesson pages.
- `src/generated/lessonIndex.js` — Generated search dataset consumed by `src/pages/search.js`.
- `src/theme/DocItem/Layout/` — Swizzled doc layout that injects the doc-state banner.
- `scripts/convert-pptx-to-md.js` — Convert PPTX → Markdown for Docusaurus.
- `scripts/generate-lesson-index.js` — Regenerates the lesson search index from EN + AZ docs.
- `scripts/check-docs-parity.js` — Verifies EN/AZ path, slug, title, and description parity.
- `scripts/lib/docs-utils.js` — Shared doc parsing helpers for repo scripts.
- `add-slugs.js` — Adds missing `slug` frontmatter to `.md` files (adds `/az` prefix under AZ path).

## Commands
- Dev server: `npm start`
- Build: `npm run build`
- Serve build: `npm run serve`
- Deploy: `npm run deploy` (GitHub Pages)
- Search index: `npm run generate-search-index`
- Parity check: `npm run check-docs-parity`
- i18n: `npm run write-translations`
- Housekeeping: `node add-slugs.js` (fills missing `slug` in EN + AZ)
- `npm start`, `npm run build`, and `npm run serve` automatically run search-index generation and EN/AZ parity checks first.

## Content Layout & Conventions
- Categories (English):
  - `red-teaming/`, `blue-teaming/`, `grc/`, `general-security/`,
    `certifications/`, `operating-systems/windows/`, `operating-systems/linux/`,
    `servers/`, `networking/`, `helpdesk-basics/`, `virtualization/`.
- File type: plain Markdown (`.md`).
- Frontmatter recommended fields:
  - `title`: Human‑readable page title
  - `description`: Short SEO/summary
  - `slug`: URL path. Prefer `/category/page` (auto‑added by `add-slugs.js` if missing)
  - Optional: `sidebar_position` for manual ordering where needed
- Translations:
  - Mirror English paths under `i18n/az/docusaurus-plugin-content-docs/current/`.
  - `add-slugs.js` auto‑prefixes AZ slugs with `/az` when missing.
- Images: place under `static/img/...`; reference with `/img/...` in Markdown.

## Common Tasks (Do This)
- Add a new English lesson:
  - Create `docs/<category>/<page>.md` with frontmatter (see above).
  - Prefer hyphen‑case filenames; keep directory names lowercase.
  - Run `node add-slugs.js` if you didn’t provide `slug`.
- Add an AZ translation:
  - Create `i18n/az/docusaurus-plugin-content-docs/current/<category>/<page>.md` mirroring EN.
  - Run `node add-slugs.js` to populate `/az/...` slugs.
- Add a new category:
  - Create `docs/<new-category>/` and populate `.md` files.
  - Update `sidebars.js` by adding a category with `{ type: 'autogenerated', dirName: '<new-category>' }`.
- Convert PPTX → Markdown:
  - `node scripts/convert-pptx-to-md.js <input.pptx> <optionalOutPath.md>`
  - Default output: `docs/networking/computer-networking.md` with basic frontmatter.

## Guardrails (Please Follow)
- Don’t recursively scan the repo; rely on this AGENTS.md and the paths above.
- Don’t touch `baseUrl`, `organizationName`, `projectName` in `docusaurus.config.js` without explicit request.
- Don’t edit generated/derived folders: `build/`, `.docusaurus/`, or `node_modules/`.
- Keep i18n directory structure in sync with `docs/`.
- Match existing naming: lowercase, hyphenated directories and filenames.
- When a user asks for applied site changes, the default close-out flow is: local validation, commit, push, let GitHub Pages deploy, then verify the live site directly.

## Quick Map (Top Level)
- `docs/` — English docs (intro, categories listed above)
- `i18n/az/.../current/` — Azerbaijani docs
- `static/` — Images and static files
- `src/` — Theme/CSS customizations
- `scripts/` — Utility scripts (PPTX → MD)
- `add-slugs.js` — Slug autofill script
- `sidebars.js` — Sidebar structure (autogenerated per directory)
- `docusaurus.config.js` — Site configuration

## Current Snapshot (refreshed 2026-03-23)
- Top-level working files of interest: `README.md`, `AGENTS.md`, `docusaurus.config.js`, `sidebars.js`, `package.json`, `add-slugs.js`, `scripts/convert-pptx-to-md.js`, `scripts/generate-lesson-index.js`, `scripts/check-docs-parity.js`, `src/css/custom.css`, `src/pages/search.js`, `src/generated/lessonIndex.js`, `.github/workflows/deploy.yml`, `.github/workflows/ci.yml`.
- English docs currently contain 43 markdown pages total: 2 root pages + 12 overview pages + 29 lesson pages.
- Azerbaijani docs mirror the English set 1:1 with 43 markdown pages under `i18n/az/docusaurus-plugin-content-docs/current/`.
- Sidebar categories in `sidebars.js` now open with overview docs before autogenerated lesson lists.
- Root note `dns.md` has been absorbed into `docs/networking/dns.md` and `i18n/az/docusaurus-plugin-content-docs/current/networking/dns.md`; no standalone root markdown note remains.

### Current English Content Inventory
- Root pages: `docs/intro.md`, `docs/about.md`
- Overview pages: `docs/red-teaming-overview.md`, `docs/blue-teaming-overview.md`, `docs/grc-overview.md`, `docs/general-security-overview.md`, `docs/certifications-overview.md`, `docs/operating-systems-overview.md`, `docs/windows-overview.md`, `docs/linux-overview.md`, `docs/servers-overview.md`, `docs/networking-overview.md`, `docs/helpdesk-basics-overview.md`, `docs/virtualization-overview.md`
- `docs/red-teaming/`: `initial-access.md`
- `docs/blue-teaming/`: `log-analysis.md`
- `docs/grc/`: `policies.md`
- `docs/general-security/`: `open-source.md`, `raid.md`
- `docs/certifications/`: `aws-certifications.md`, `comptia-certifications.md`, `microsoft-certifications.md`
- `docs/operating-systems/linux/`: `basic-commands.md`
- `docs/operating-systems/windows/`: `applocker.md`, `bitlocker.md`, `powertoys.md`, `run-commands.md`, `services.md`, `sysprep.md`, `wsl.md`
- `docs/servers/`: `fsmo.md`, `jump-server.md`, `laps.md`, `windows-server-planning.md`
- `docs/servers/`: `active-directory-domain-services.md`, `fsmo.md`, `group-policy.md`, `jump-server.md`, `laps.md`, `windows-server-planning.md`, `windows-server-tools.md`
- `docs/networking/`: `network-types.md`, `dns.md`, `dhcp.md`
- `docs/helpdesk-basics/`: `common-helpdesk-tickets.md`
- `docs/virtualization/`: `virtualization-basics.md`, `vmware-virtualization.md`

### Current Azerbaijani Content Inventory
- Root pages: `intro.md`, `about.md`
- Root overview pages mirror the English `*-overview.md` files 1:1
- Mirrored categories present: `red-teaming`, `blue-teaming`, `grc`, `general-security`, `certifications`, `operating-systems/linux`, `operating-systems/windows`, `servers`, `networking`, `helpdesk-basics`, `virtualization`
- Mirror status at time of snapshot: complete parity with English file paths and filenames
- Server category now also includes `servers/active-directory-domain-services.md` in both locales.

### Config and Behavior Notes
- `docusaurus.config.js` uses Docusaurus v3 classic preset with docs at site root via `routeBasePath: '/'`.
- Live locales are `en` and `az`; locale dropdown is enabled in the navbar.
- Docs config also uses `editLocalizedFiles: true` so localized pages should point to localized source files in GitHub edit links.
- GitHub Pages deployment values are set and should not be changed without explicit request: `url`, `baseUrl`, `organizationName`, `projectName`.
- Navbar currently links `Start`, `About`, `Search`, locale switcher, and GitHub.
- `onBrokenLinks` and broken markdown link handling are both set to `warn`, not `throw`.
- Search is static and generated from docs frontmatter/content metadata rather than a third-party indexing service.
- Docs may expose `status` and `last_reviewed` frontmatter; if omitted, the UI/script layer infers a sensible default status.

### Assets and Scripts Snapshot
- Static assets currently include `static/img/shield.png`, `static/img/docusaurus-social-card.jpg`, default Docusaurus SVG assets, server diagrams in `static/img/servers/`, and the DNS flow diagram in `static/img/networking/dns-resolution-flow.svg`.
- Extra static files present: `static/.nojekyll`, `static/robots.txt`, `static/google1e1784564b8512db.html`.
- Styling is centralized in `src/css/custom.css`.
- Custom client-side discovery UI lives in `src/pages/search.js` and reads its dataset from `src/generated/lessonIndex.js`.
- `scripts/generate-lesson-index.js` walks both locale trees and writes the generated dataset used by the search page.
- `scripts/check-docs-parity.js` fails when an EN doc is missing an AZ counterpart or when slug/title/description parity breaks.
- `src/theme/DocItem/Layout/` injects the doc-state banner so pages can surface maturity and review metadata.
- `add-slugs.js` recursively walks both EN and AZ docs trees and adds missing `slug` values; AZ slugs are prefixed with `/az`.
- `scripts/convert-pptx-to-md.js` extracts slide text from a `.pptx` and writes Markdown to `docs/networking/computer-networking.md` by default.

### Session Bootstrap Rule
- On a new session, trust this snapshot first and only inspect files relevant to the requested task.
- If structure/content appears to have changed, update this `Current Snapshot` section before doing broader discovery so future sessions inherit the new map.

If repo structure changes, update this AGENTS.md first so future runs skip re‑discovery.
